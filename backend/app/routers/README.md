# API Routers

This directory contains the API router modules for the Prompt Pilot backend.
Each file typically groups related endpoints.

- `auth.py`: Handles authentication-related routes like `/login` and `/register`.
- `users.py`: Handles user-specific routes like `/users/me`.
